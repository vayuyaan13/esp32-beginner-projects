# ESP32 Web Server

An ESP32 can host a lightweight local web interface.

## Project Flow

Browser → Wi-Fi Router → ESP32 Web Server → GPIO / Sensor

Possible beginner features:

- Show sensor readings
- Turn an LED on and off
- Display device status
- Add simple control buttons

Use authentication and stronger security before exposing an embedded web server beyond a trusted local network.
